# Ampersand top-loop revision

The upper-right connecting arc of `ampersand` was removed so the top bowl reads as an open curved stroke with two terminals. The opening faces right and slightly upward; it is not a centered horizontal notch. The upper negative space is enlarged gradually in heavier weights. All other customized punctuation and zero changes are retained.
